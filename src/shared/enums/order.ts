export enum EStatusOrder {
  PENDING = 'pending',
  PROCESSING = 'processing',
  DELIVERING = 'delivering',
  DELIVERED = 'delivered',
  SUCCESS = 'success',
  CANCELLED = 'cancelled',
  REQUEST_RETURN = 'request_return',
  RETURNING = 'returning',
  RETURNED = 'returned'
}
